(function (nx, global) {
    /**
     * @class nx.graphic.topology.model.TopologyVertex
     * @module nx.graphic.topology
     */
    nx.define("nx.graphic.topology.model.TopologyVertex", nx.data.ObservableObject, {
        properties: {
            position: {}
        }
    });

})(nx, nx.global);
